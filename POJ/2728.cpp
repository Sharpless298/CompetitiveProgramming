#include <iostream>
#include <queue>
using namespace std;

struct Node {
	int x, y, z;
};

int N;
priority_queue<> pq;
Node node[1005];

signed main() {
	ios_base::sync_with_stdio(false);
	cin.tie(nullptr);

	cin >> N;
	for(int i=0; i<n; i++) {
				
	}
}
