#include <iostream>
using namespace std;

typedef long long int lli;

const int MAXN = 500000;

int h[MAXN+5], b[MAXN+5];
deque<int> dq;

signed main() {
	ios_base::sync_with_stdio(false);
	cin.tie(nullptr);

	cin >> n >> k;
	for(int i=0; i<n; i++)
		cin >> h[i];
	for(int i=0; i<n; i++)
		cin >> b[i];
	
}
